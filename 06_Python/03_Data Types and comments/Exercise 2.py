## Use the "input()" function to get input from the user. See what type of data the output of this function is.

input = input()         ## Adding variable, that a user has to put in.
input = int(input)
print(type(input))      ## Checking what type of data the input is stored as.

## All the input values are stored as Strings(text).